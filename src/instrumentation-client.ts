// Client-side instrumentation for application monitoring
// Removed Sentry integration - can be used for other monitoring tools if needed

// Initialize client-side monitoring here if needed
console.log('Client instrumentation initialized');

export const onRouterTransitionStart = () => {
  // Basic router transition tracking - replace with your preferred analytics
  console.log('Router transition started');
};
